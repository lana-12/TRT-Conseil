{% extends 'base.html.twig' %}

{% block title %}Accueil |
	{{parent()}}
{% endblock %}


{% block body %}
	{{ parent() }}

	<main class="container mt-3">
		

		<section class="container">
			<div class="row">
				<div class="col">
					<h2>{{ titlepage }}</h2>

					{# {% for message in app.flashes('success') %}
										<div class="alert alert-success mt-4 text-center">
											{{ message }}
										</div>
									{% endfor %} #}

					{{ form_start(recruiterForm) }}

					<fieldset class="mb-3">
						<legend>Mon Compte de connexion</legend>
						{{ form_row(recruiterForm.nameCompany,{'label':'Nom de l\'entreprise',}) }}

						{{ form_row(recruiterForm.address, {'label':'Adresse','attr': {'placeholder':'1 av du parc'}}) }}
						{{ form_row(recruiterForm.zipCode, {
										label: 'Code postal','attr': {'placeholder':'34000'}
									}) }}
						{{ form_row(recruiterForm.city, {
										label: 'Ville', 'attr': {'placeholder':'Montpellier'}
									}) }}

					{# {% if app.user %}
						{{ form_row(recruiterForm.user, {
										label: 'Compte utilisateur',value: '{{ app.user.email }}'
									}) }}
						
					{% endif %} #}

					</fieldset>

					<button type="submit" class="btn btn-lg btn-info my-3">Enregistrer</button>
					{{ form_end(recruiterForm) }}

				</div>

			</div>
		</section>

	</main>
{% endblock %}

