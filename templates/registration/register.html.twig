{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
{{ parent() }}


    <section class="container">
        <div class="row">
            <div class="col">
                <h1>Inscription</h1>

                {# {% for message in app.flashes('success') %}
                    <div class="alert alert-success mt-4 text-center">
                        {{ message }}
                    </div>
                {% endfor %} #}

            {{ form_start(registrationForm) }}

                <fieldset class="mb-3">
                    <legend>Mon Compte de connexion</legend>
                        {{ form_row(registrationForm.roles,{'label':'Vous êtes un : ',}) }}
                        {{ form_row(registrationForm.email, {'label':'E-mail','attr': {'placeholder':'exemple@exemple.fr'}}) }}
                        {{ form_row(registrationForm.password, {
                                label: 'Mot de passe'
                            }) }}
                        {{ form_row(registrationForm.rgpdConsent, {
                                label: 'En m\'inscrivant sur ce site, j\'accepte voir ce que je mets'
                            }) }}
                
                </fieldset>

                <button type="submit" class="btn btn-lg btn-info my-3">M'inscrire</button>
                {{ form_end(registrationForm) }}
                
            </div>

        </div>
    </section>

    
{% endblock %}
